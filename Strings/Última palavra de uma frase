nome = input()
list_nome = list(nome.split()) #Separamos o nome e transformamos-o em uma lista
print(list_nome[-1]) #Pega o  ultimo elemento de uma lista
#Exemplo de caso: 
#nome = Eduardo da Silva
#list_nome = ["Eduardo","da","Silva] #Uma lista de valores, no qual o primeiro elemento eh o 0 e o ultimo eh o 2.
#Porem, no python podemos usar "slicing" para obter determinados valores de uma lista.
#list_nome[-1] eh um "slicing" na qual representa sempre o ultimo elemento de uma lista.

 
